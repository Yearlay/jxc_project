# 采购业务员

**功能**：实现三级菜单页面
- 系统维护 → 基础信息设置 → 采购业务员

---
# 输入
1）UI页面风格参考 `AreaManage.vue`（增删改查列表页），无状态列。
2）[采购业务员]字段说明：``、``。
3）[采购业务员]业务说明：。

# 实现
## step 1: 在 `pg` 目录下，更新【采购业务员】相关的表结构
参考[采购业务员]字段说明，设计[采购业务员]数据库中的表结构。

## step 2: 后端 `jxcServer` 工程，创建【采购业务员】相关 API
参考[采购业务员]业务说明，设计[采购业务员]相关业务的接口。

## step 3: 前端 `jxcClient` 实现 "采购业务员" 菜单页面
UI页面风格参考 `AreaManage.vue`（增删改查列表页），无状态列。

# 提示词
依据`tools/008-采购业务员.md` 的内容，将内容做补全和完善，并写入到新文件 `tools/008-采购业务员-AI.md`。 
按照 `tools/008-采购业务员-AI.md` 的步骤，逐步进行AI coding。